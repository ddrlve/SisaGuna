package com.sisaguna.android.ui.theme

import androidx.compose.material3.Typography

// [Guessing] No font name/scale confirmed from Figma yet — using Material3 defaults
// (system font). Send font name + type scale from Figma (click any text, right panel)
// to replace this with the real type ramp.
val SgTypography = Typography()
